backfill=0
# Commit a change to this file to trigger an off-schedule GSC collection.
# First number = weeks to backfill. Add the word "force" ONLY after expanding
# the tracked course set: it re-collects existing weekly snapshots from the
# GSC API so newly tracked pages gain their historical rows.
